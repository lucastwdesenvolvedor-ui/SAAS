generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model GiftCard {
  id            Int      @id @default(autoincrement())
  nome          String
  preco         Float
  desc          String?
  dataExpiracao DateTime
  img           String
}
